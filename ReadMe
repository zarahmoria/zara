-- ajout d'un second commentaire
cd Jenkins-docker

-- creation du repository distant de test
git init
git add .
git commit -m "init depot"

-- Aller sur https://github.com/fpicot31 et creer un nouveau depot

git remote add origin https://github.com/fpicot31/Jenkins-docker.git

git push -u origin master

-- Aller sur localhost:8080 et creer le pipeline

-- si pb git
sudo apt-get purge git
sudo apt-get autoremove
sudo apt-get install git

-- modification et push
git  commit -am "mon commentaire"
git push
